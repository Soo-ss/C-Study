
#include <stdio.h>

int main()
{
	double p1, p2, a = (5 / 6.0)*(5 / 6.0)*(5 / 6.0)*(5 / 6.0)*(5 / 6.0)*(5 / 6.0), b = (5 / 6.0)*(5 / 6.0)*(5 / 6.0)*(5 / 6.0)*(5 / 6.0);

	p1 = 1 - a;
	p2 = 1 - a * a - 2 * a * b;

	printf("%lf %lf", p1, p2);

	return 0;
}